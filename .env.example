# Example environment variables for local development
# Copy this file to `.env` and replace the placeholder values with your real keys.

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_publishable_key_here
VITE_SUPABASE_JWT_KEY=your-jwt-key

# Notes:
# - Do NOT commit the real `.env` file to version control. `.env` is already listed in `.gitignore`.
# - The anon key is intended for client-side use. Keep service/secret keys out of client code.
# - After updating `.env`, restart the dev server so Vite picks up the new values.
